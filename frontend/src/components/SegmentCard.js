/*
import styled from 'styled-components/macro'
export default function SegmentCard({segmentItemData}) {
    return (
            <SegmentCard>
             <span>title={"Title: " + segment.segmentName}</span>
             <span>content={"Content: " + segment.segmentContent}</span>
            <SegmentCard/>
            )}

            const SegmentCard = styled.section`
            display: flex;
            flex-direction: column;
            align-items: center;
            overflow-y: scroll;
            padding-top: 0.5em;
            img {
            width: 96px;
            height: 96px;
            border-radius: 50%;
        }
            .user-name {
            margin - left: 16px;
        }
            `
*/
